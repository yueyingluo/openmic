# Copy to .env locally. Never commit real keys.
# OpenAI-compatible endpoint. DeepSeek is a recommended baseline.
OPENMIC_API_KEY=
OPENMIC_BASE_URL=https://api.deepseek.com
OPENMIC_MODEL=deepseek-chat
